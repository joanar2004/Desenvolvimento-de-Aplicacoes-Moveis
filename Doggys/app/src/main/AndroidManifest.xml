<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dam.A50829.dogbreedxplorer">

    <!-- Step 1 require Internet Permission -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/image_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/image_app"
        android:supportsRtl="true"
        android:theme="@style/Theme.DogBreedsExplorer">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ImageDetailsActivity" />
    </application>

</manifest>
